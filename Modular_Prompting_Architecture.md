# 《模組化組裝方法論》
## 🧭 一、為什麼要「先建好資料結構再串 VS Code」

### 🎯 目標不是「開始寫程式」，而是「建立一個治理架構」

這個方法論的靈魂在於「分層治理」：

* Core（邏輯與資產層）
* Layer（開發框架層）
* Project（應用實作層）
* Docs（方法與規範層）

要讓這套系統有生命，第一步不是寫程式，而是**建立資料結構**，讓每個檔案有「家」。

💡 就像你在蓋房子：

> VS Code 是工具箱，
> GitHub 是倉庫，
> 資料結構是建築藍圖。

---

## 🧩 二、正確的實作順序

### Step 1️⃣：在 GitHub 上建立 **主倉庫結構**

建議名稱（例如）：

```
modular-ai-system/
```

初始結構：

```
modular-ai-system/
 ├─ core/
 │   ├─ code/
 │   └─ prompts/
 ├─ dev/
 │   ├─ layers/
 │   └─ projects/
 └─ docs/
     └─ methods/
```

**📌 小原則：**

* 每一層放在一個獨立資料夾，彼此不要交叉引用實體檔案。
* 預先建立空資料夾（即使還沒內容）也能讓結構清晰。
* 使用 README.md 標明用途（例如 `core/code/README.md` 說明該層負責什麼）。

---

### Step 2️⃣：在本地用 VS Code 開啟此 GitHub 倉庫

```bash
git clone https://github.com/<your-org>/modular-ai-system.git
code modular-ai-system
```

然後設定 VS Code 工作區：

```
.vscode/
 ├─ tasks.json     ← 定義 build/publish/preview 等任務
 ├─ launch.json    ← 定義偵錯組裝與測試流程
 └─ settings.json  ← JSON Schema 驗證 + 路徑設定
```

這樣 VS Code 就能成為「你的方法論 IDE」。

---

### Step 3️⃣：建立最小骨架

| 層級          | 目錄                        | 最少應有內容                                    |
| ----------- | ------------------------- | ----------------------------------------- |
| **Core**    | `core/code/`              | `README.md` + 一個模組 (`example_service.py`) |
|             | `core/prompts/`           | `components/` + `gems/` + `tools/`        |
| **Layer**   | `dev/layers/`             | `foundation/` + `logic/` + `interface/`   |
| **Project** | `dev/projects/proj-demo/` | `app/`, `prompts/`, `ssot/schema.json`    |
| **Docs**    | `docs/methods/`           | 四份方法論文件                                   |

---

### Step 4️⃣：在 VS Code 裡連接各層

| 動作        | VS Code 對應方式                           |
| --------- | -------------------------------------- |
| 組裝 Prompt | 在 `tasks.json` 新增 `build_prompt.py` 任務 |
| 組裝 Code   | 加上 `build_code.py` 任務                  |
| 預覽 Prompt | 指向 `prompt_preview.py`                 |
| 測試        | 整合 Python Testing 面板 (`pytest`)        |
| 發佈        | Command Line 或 GitHub Action 自動化       |
| 檢查架構      | 開啟 `prompt_catalog.html` 預覽            |

---

### Step 5️⃣：上傳與串接

```bash
git add .
git commit -m "chore: initialize modular ai system skeleton"
git push origin main
```

GitHub 上就會有完整結構，
VS Code 直接成為開發與管理的操作前端。

---

## ⚙️ 三、當 GitHub（遠端）＋ VS Code（地端） 串起來之後

| 面向         | 具體能力                                  |
| ---------- | ------------------------------------- |
| 🧠 方法論落地   | 四層結構具體存在，每層各司其職                       |
| 🔁 組裝管線可視化 | build_prompt、build_code、publish 可直接操作 |
| 🔒 版本治理    | 所有 GEM、Recipe、Lockfile 都在 Git 上       |
| 🧩 模組共享    | Core 與 Layer 可同時供多專案引用                |
| 📊 可觀測性    | prompt_catalog、diff、preview 都能本地即時檢視  |
| 🧪 可測試     | pytest 整合 + E2E 驗證完整                  |
| 🚀 可發佈     | CI/CD pipeline 直接跑組裝與測試               |

---

## 🧠 四、建議工作流（你未來每天的操作會長這樣）

1. **在 GitHub 上建立分支**：`feature/new-gem-summary`
2. **在 VS Code 中編輯**：

   * 修改 GEM (`core/prompts/gems/`)
   * 寫 Component (`core/prompts/components/`)
   * 測試 (`dev/projects/proj-demo/tests/`)
3. **本地組裝與預覽**

   ```bash
   python core/prompts/tools/build_prompt.py --recipe dev/projects/proj-demo/prompts/recipes/spec_summary.json
   ```
4. **VS Code 預覽成品**（使用 Markdown Preview）
5. **提交並推送**

   ```bash
   git commit -am "feat: add summary gem"
   git push origin feature/new-gem-summary
   ```
6. **GitHub Action 自動跑 build + test**
7. **審查通過後合併到 main**

---

## 🧱 五、關鍵概念整理

| 元件         | 說明            | 儲存位置                                                  |
| ---------- | ------------- | ----------------------------------------------------- |
| Core Code  | 程式邏輯基礎        | `core/code/`                                          |
| Core GEM   | Prompt 模組     | `core/prompts/`                                       |
| Layer      | 框架與流程         | `dev/layers/`                                         |
| Project    | 實際應用          | `dev/projects/`                                       |
| Build 組裝成品 | 程式或 Prompt 結果 | `dev/projects/<proj>/app/build` / `compiled_prompts/` |
| Docs       | 方法論與規範        | `docs/methods/`                                       |

---

✅ **一句話總結：**

> 你該先在 GitHub 建好完整資料結構（Core → Layer → Project → Docs），
> 然後用 VS Code 當作「操作介面」──
> 透過它來組裝、預覽、測試、發佈整個系統。

---

是否希望我幫你產出一份
📘《初始化架構操作手冊》（Markdown 格式），
內容包含：

* GitHub 資料夾初始化指令
* VS Code 任務設定樣板
* 第一個 Project + GEM 的建置步驟
  讓你一建立 repo 就能直接跑起來？
